{% extends 'VictoireCoreBundle:Widget:show.html.twig' %}

{% block content %}
    {{ date|localizeddate(dateFormat|default('none'), timeFormat|default('none'), app.request.locale, timezone|default("Europe/Paris"), format) }}
{% endblock content %}
